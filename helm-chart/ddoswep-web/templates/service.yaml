apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddoswep-web.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "ddoswep-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "ddoswep-web.selectorLabels" . | nindent 4 }}
