apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddoswep-api.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "ddoswep-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "ddoswep-api.selectorLabels" . | nindent 4 }}
